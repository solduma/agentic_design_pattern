# 에이전트 디자인 패턴

에이전트 디자인 패턴에 대한 설명입니다.

## 프롬프트 체이닝

프롬프트 체이닝은 강력한 기법입니다.

```python
def chain(prompts):
    results = []
    for prompt in prompts:
        results.append(process(prompt))
    return results
```

위 코드는 체이닝의 예시입니다.

이 패턴을 사용하면 복잡한 작업을 효율적으로 처리할 수 있습니다.
